<% provide(:title, 'Task Manegement Tool') %>
<h1>タスク一覧</h1>
    <% @tasks.each do |task| %>
      <div class="tasks-index-item">
        <div class="tasks">
            <%= task.name %>
          </div>
            <%= task.detail %>
        </div>
    <% end %>
