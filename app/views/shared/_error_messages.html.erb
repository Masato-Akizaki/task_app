<% if @task.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-danger">
      エラーが<%= @task.errors.count %>つ発生しました。
    </div>
    <ul>
    <% @task.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>